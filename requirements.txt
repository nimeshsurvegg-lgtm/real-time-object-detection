---

### 2. `requirements.txt`
This file allows other developers to install all the exact libraries your script needs with a single command (`pip install -r requirements.txt`).

```text
ultralytics>=8.0.0
opencv-python>=4.5.0
pyttsx3>=2.90
Pillow>=9.0.0
